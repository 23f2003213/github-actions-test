name: Test Email Step Action

on:
  workflow_dispatch:  # allows manual triggering

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f2003213@ds.study.iitm.ac.in
        run: echo "Hello, world!"
